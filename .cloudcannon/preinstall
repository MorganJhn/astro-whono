#!/bin/bash
# Avoid npm 10.9.4 "Exit handler never called" bug by installing newer npm globally
# The --force flag prevents interactive prompts that could hang
npm install -g npm@11.12.1 --force --no-audit --no-fund
